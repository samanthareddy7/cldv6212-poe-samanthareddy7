@model IEnumerable<ABC_Retail_Part1.Models.Product>

<h2>Products</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Add New Product</a>
</p>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Price</th>
            <th>Image</th>
            <th>Actions</th> <!-- New column for buttons -->
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ProductName</td>
                <td>@item.Description</td>
                <td>@item.Price</td>
                <td>
                    @if (!string.IsNullOrEmpty(item.ImageUrl))
                    {
                        <img src="@item.ImageUrl" alt="Product Image" width="100" />
                    }
                </td>
                <td>
                    <a asp-action="Details" asp-route-rowKey="@item.RowKey" class="btn btn-info btn-sm">Details</a>
                    <a asp-action="Edit" asp-route-rowKey="@item.RowKey" class="btn btn-primary btn-sm">Edit</a>
                    <a asp-action="Delete" asp-route-rowKey="@item.RowKey" class="btn btn-danger btn-sm">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
